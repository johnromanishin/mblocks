#ifndef INTERACTION
#define INTERACTION

/*
 * There is a distance sensor - a Vl6810X attached to the circuit board
 * it can be used as a
 */
int initializeRangeSensor();
int readRangeSensor();

#endif
